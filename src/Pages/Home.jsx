import styled from "styled-components";
import {HomeTemplate} from "../index"


export function Home() {
        return (
        <>
        {/* {user.length > 0 ? <HomeTemplete setUser={setUser} /> : <Homadmin />} */}
        <HomeTemplate/>
        </>)
}
const Container = styled.div`
`